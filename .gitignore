.cache
.DS_Store
.idea
*.log
*.tgz
*.pem
coverage
dist
bin/mail-server*
lib-cov
logs
node_modules
temp
docs/.vitepress/cache
# Root-level only — packages/ts-maps/src/core-map/storage/ is legit source
/storage
/pantry
.zed
.cursor
.stx
pantry
packages/ts-maps/bench/reports/*.json
.render-check/

# Stray .d.ts files that escape into src/ during local builds.
# Source is .ts only — declarations are emitted to dist/ at publish time.
packages/*/src/**/*.d.ts
